git config -- global user.email ""
git config -- global user.name ""

# first
git init

# must know
git add .
git commit -m "sadasd"
git push

# get status
git status

# see whats changes
git log

# change message
git commit -m "sadasd" --amend
